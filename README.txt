!pip install tensorflow==2.12.1
!pip install keras==2.12
!pip install pandas

How to use:

#ship.bch (problem) in main.py folder
~python main.py ship.bch

#can also use ibex params
~python main.py ship.bch --random-seed=5 --timeout=1200
